<div>
    <div class="mb-3">Exhibits:</div>
    <ul>
        {{#each exhibits}}
        <li>
            <div>
                <em>"{{name}}",</em>
                {{#if artist}}
                {{artist}}
                {{else}}
                Unknown artist
                {{/if}}
            </div>

            <div>
                <p><em>{{description}}</em></p>
            </div>

            <div class="mb-3">
                {{#if audio_guide includeZero=true}}
                (<b>Audio guide is avaliable</b>)
                {{else}}
                (<b>Audio guide is not avaliable</b>)
                {{/if}}
            </div>
        </li>
        {{/each}}
    </ul>
</div>